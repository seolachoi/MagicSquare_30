# Report 03 — Development Environment & Cursor Rules

## 환경

- Python 3.10+
- pytest + pytest-cov
- pydantic v2 (Boundary schemas)
- PEP8, type hints, Google docstring (public API)

## ECB 폴더

```
src/
  boundary/   # 검증, envelope, UI
  control/    # 오케스트레이션
  entity/     # 도메인 서비스, 상수
tests/
  boundary/
  entity/
```

## TDD 규칙 요약

- RED: 실패 테스트 먼저
- GREEN: 최소 구현만, 리팩터 금지
- REFACTOR: 계약 불변, GM 회귀

## 금지

- `print()` 디버깅
- 테스트 약화/skip
- Entity → Boundary import
- 매직 넘버 `4`, `34` (상수 SSOT 사용)

상세: `.cursorrules`
